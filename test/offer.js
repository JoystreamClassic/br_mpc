/**
 * Tests for offer class
 * Created by Bedeho Mender on 02.07.2014.
 */

    /*
var message_tester = require('./message_tester');

// Fixture file only test .price explicitly, but since same checks
// on the two other fields, that is fine for now.

// Test offer class
message_tester('offer');
        */